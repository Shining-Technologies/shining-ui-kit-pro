---
---

No version bump: nothing is on npm yet, so this work ships as part of the initial 0.1.0
release rather than as a change to it. It covers the publishing setup — package metadata,
per-package READMEs, rolled-up type declarations with CommonJS twins, the pre-publish
preflight, Changesets and the release workflow — plus the documentation reorganisation.
